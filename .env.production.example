# ── InnoClaw Docker Environment ──────────────────────────────────
# Copy this file to .env.production.local and fill in your values:
#   cp .env.production.example .env.production.local

# [Required] Workspace root directories (comma-separated paths INSIDE the container)
# These must match the volume mounts in docker-compose.yml.
# Default compose mount maps HOST_WORKSPACE_PATH → /research
WORKSPACE_ROOTS=/research

# [Optional] SQLite database path (default: ./data/innoclaw.db, persisted via Docker volume)
# DATABASE_URL=./data/innoclaw.db

# [Optional] Authentication mode (default: local)
# AUTH_MODE=disabled gives anyone who can reach the service admin-level access.
# Use it only behind trusted network access or another access-control layer.
# AUTH_MODE=local

# [Recommended for multi-user deployments] Enforce one bootstrapped administrator.
# Public registration creates ordinary users only while this is true.
# AUTH_SINGLE_ADMIN=true

# [Optional] Override session Cookie Secure.
# Set false only for a temporary plain-HTTP deployment; restore true with HTTPS.
# AUTH_COOKIE_SECURE=true

# [Required in production] Long random session-signing secret.
# Generate with: openssl rand -base64 32
# AUTH_SECRET=replace-with-a-long-random-secret

# ── AI API Keys (at least one required) ──────────────────────────
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...

# ── Custom API Base URLs (optional, for proxies) ─────────────────
# OPENAI_BASE_URL=https://api.your-provider.com/v1
# ANTHROPIC_BASE_URL=https://api.your-provider.com
# GEMINI_BASE_URL=https://api.your-provider.com

# ── Embedding (optional, falls back to OPENAI_API_KEY) ───────────
# EMBEDDING_API_KEY=sk-...
# EMBEDDING_BASE_URL=https://api.your-provider.com/v1
# EMBEDDING_MODEL=text-embedding-3-small

# ── Default LLM (overridable in Settings UI) ─────────────────────
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o

# OpenAI reasoning effort: none|minimal|low|medium|high|xhigh.
# ultra is accepted as an operator alias for xhigh.
# OPENAI_REASONING_EFFORT=high

# ── Agent settings ────────────────────────────────────────────────
# AGENT_MAX_STEPS=10

# ── GitHub (optional, for private repo cloning) ──────────────────
# GITHUB_TOKEN=ghp_...

# ── HuggingFace (optional) ───────────────────────────────────────
# HF_TOKEN=hf_...
# HF_DATASETS_PATH=./data/hf-datasets
# HF_MIRROR=https://hf-mirror.com

# ── HTTP Proxy (optional) ────────────────────────────────────────
# HTTP_PROXY=http://your-proxy:3128
# HTTPS_PROXY=http://your-proxy:3128
# NO_PROXY=localhost,127.0.0.1

# ── Feishu Bot (optional) ────────────────────────────────────────
# FEISHU_BOT_ENABLED=true
# FEISHU_APP_ID=cli_xxxx
# FEISHU_APP_SECRET=xxxx
# FEISHU_VERIFICATION_TOKEN=xxxx
# FEISHU_ENCRYPT_KEY=xxxx
# FEISHU_PUSH_SECRET=xxxx

# ── Additional providers (optional) ──────────────────────────────
# SHLAB_API_KEY=...
# QWEN_API_KEY=...
# MOONSHOT_API_KEY=...
# DEEPSEEK_API_KEY=...
# MINIMAX_API_KEY=...
# ZHIPU_API_KEY=...
